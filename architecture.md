<div dir="rtl">

# 🏗️ معماری پروژه

## مقدمه

این سند، معماری فعلی پروژه، تصمیمات فنی و دلایل انتخاب هر بخش را توضیح می‌دهد. این فایل یک **Living Document** است؛ بنابراین با تغییر معماری یا اتخاذ تصمیمات جدید، باید به‌روزرسانی شود تا همواره وضعیت واقعی پروژه را منعکس کند.

---

# 📖 نمای کلی پروژه

این پروژه یک فروشگاه اینترنتی است که با **Django** و بر پایه معماری **Monolithic Server-Side Rendered (SSR)** توسعه داده می‌شود.

تمام صفحات برنامه، از جمله:

- صفحه اصلی
- لیست محصولات
- جزئیات محصول
- سبد خرید
- فرآیند ثبت سفارش
- پرداخت

مستقیماً توسط Django و با استفاده از **Templates** در سمت سرور رندر می‌شوند و فعلا هیچ API مستقل یا Front-end مبتنی بر SPA (مانند React یا Vue) در پروژه وجود ندارد.

هدف اصلی این پروژه، یادگیری عمیق Django و طراحی صحیح یک برنامه وب واقعی است؛ بنابراین سادگی، خوانایی و قابلیت نگهداری نسبت به استفاده از معماری‌های پیچیده‌تر در اولویت قرار دارد.

---

# 🎯 اهداف معماری

معماری پروژه با اهداف زیر طراحی شده است:

- یادگیری عمیق Django و اکوسیستم آن
- توسعه ساختاریافته و قابل نگهداری
- جداسازی مسئولیت‌ها بین بخش‌های مختلف سیستم
- امکان توسعه تدریجی بدون ایجاد پیچیدگی غیرضروری
- مستندسازی تمامی تصمیمات معماری

---

# 🏛️ الگوی معماری

پروژه از معماری **Monolith** استفاده می‌کند؛ یعنی تمامی قابلیت‌ها در یک پروژه Django قرار دارند اما هر دامنه‌ی کسب‌وکار در قالب یک اپلیکیشن مستقل پیاده‌سازی می‌شود.

مزایای این انتخاب:

- توسعه و دیباگ ساده‌تر
- استقرار (Deployment) آسان‌تر
- عدم نیاز به مدیریت زیرساخت‌های پیچیده
- استفاده کامل از قابلیت‌های داخلی Django مانند:
  - ORM
  - Authentication
  - Admin Panel
  - Template Engine
  - Forms

در مقیاس فعلی پروژه، معماری Monolith بهترین تعادل را میان سادگی و قابلیت توسعه ایجاد می‌کند.

---

# 🖥️ رویکرد SSR

این پروژه از الگوی **Server-Side Rendering (SSR)** استفاده می‌کند.

دلایل این انتخاب:

- تمرکز کامل بر یادگیری Django
- استفاده از Template Engine داخلی
- ساده‌تر شدن توسعه
- مناسب بودن برای موتورهای جستجو (SEO)
- حذف پیچیدگی نگهداری دو Codebase مجزا (Backend و Frontend)

در صورت نیاز، در آینده امکان افزودن API نیز وجود خواهد داشت، اما در فاز فعلی جزئی از معماری پروژه نیست.

---

# 🗄️ پایگاه داده

پایگاه داده اصلی پروژه **PostgreSQL** است.

دلایل انتخاب:

- سازگاری کامل با Django ORM
- پشتیبانی از روابط پیچیده
- عملکرد مناسب
- قابلیت‌های پیشرفته مانند:
  - JSONField
  - Full-text Search
  - Indexهای پیشرفته
  - Constraintها

---

# 📦 مدیریت وابستگی‌ها

مدیریت وابستگی‌های پروژه با **uv** انجام می‌شود.

مزایای استفاده از uv:

- سرعت بسیار بالاتر نسبت به pip
- مدیریت دقیق نسخه پکیج‌ها
- تولید فایل `uv.lock`
- یکسان بودن محیط توسعه و استقرار

---

# 📁 ساختار پروژه

ساختار فعلی پروژه به شکل زیر است:

<div dir="ltr">

```text
.
├── apps
│   ├── users
│   ├── catalog
│   ├── cart
│   ├── orders
│   └── payments
├── core
│   ├── settings.py
│   ├── urls.py
│   ├── asgi.py
│   └── wsgi.py
└── manage.py
```

</div>

---

# 🧩 ماژول‌های پروژه

هر اپلیکیشن مسئول یک دامنه مشخص از کسب‌وکار است.

| اپلیکیشن   | مسئولیت                                      |
| ---------- | -------------------------------------------- |
| `users`    | مدیریت کاربران، احراز هویت و پروفایل         |
| `catalog`  | محصولات، دسته‌بندی‌ها و اطلاعات فروشگاه      |
| `cart`     | مدیریت سبد خرید                              |
| `orders`   | ثبت و مدیریت سفارش‌ها                        |
| `payments` | مدیریت فرآیند پرداخت (در ابتدا به‌صورت Mock) |

این جداسازی باعث می‌شود هر بخش از سیستم مستقل‌تر توسعه یافته و نگهداری آن ساده‌تر باشد.

---

# ⚙️ تنظیمات پروژه

در وضعیت فعلی، تنظیمات Django در فایل زیر قرار دارند:

```text
core/settings.py
```

در صورت افزایش پیچیدگی پروژه، تنظیمات می‌توانند به فایل‌های جداگانه برای محیط‌های مختلف (Development، Production و Test) تفکیک شوند.

---

# 🚀 اجرای پروژه

پروژه به گونه‌ای طراحی شده که بتوان آن را در دو محیط اجرا کرد:

### اجرای محلی

- Python
- uv
- PostgreSQL

### اجرای با Docker

- Django
- PostgreSQL
- Docker Compose

بدون نیاز به تغییر در کد برنامه، تنها با تغییر تنظیمات محیطی امکان جابه‌جایی بین این دو روش وجود خواهد داشت.

---

# 🗃️ مدل داده

مدل داده پروژه به مرور تکمیل و در این بخش مستندسازی خواهد شد.

مدل‌های اصلی عبارت‌اند از:

- User
- Category
- Product
- ProductImage
- Cart
- CartItem
- Order
- OrderItem
- Payment

با اضافه شدن مدل‌های جدید یا تغییر روابط، این بخش به‌روزرسانی می‌شود.

---

# 📌 تصمیمات معماری

تصمیمات مهم معماری پروژه در این سند ثبت می‌شوند تا دلیل انتخاب هر راهکار در آینده نیز مشخص باشد.

نمونه تصمیمات فعلی:

- استفاده از معماری Monolith
- استفاده از Server-Side Rendering
- انتخاب PostgreSQL به‌عنوان پایگاه داده
- استفاده از uv برای مدیریت وابستگی‌ها
- جداسازی دامنه‌های کسب‌وکار در قالب Django Apps

---

# 🔮 تصمیمات آینده

برخی از تصمیمات هنوز نهایی نشده‌اند و در مراحل بعدی توسعه بررسی خواهند شد:

- پیاده‌سازی سبد خرید مبتنی بر Session یا Database
- استفاده از درگاه پرداخت واقعی یا Mock
- اضافه شدن Redis برای Cache یا Session
- استفاده از Celery برای پردازش‌های پس‌زمینه
- استراتژی ارسال ایمیل و Notification

این بخش با پیشرفت پروژه تکمیل خواهد شد.

</div>
